<UserControl x:Class="UBB_SE_2024_Gaborment.MVVM.View.FriendView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UBB_SE_2024_Gaborment.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Beige">
    <Grid Background="WhiteSmoke">
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Left Side Navigation Menu -->
            <StackPanel Grid.Column="0" Orientation="Vertical" MinWidth="200" HorizontalAlignment="Left" VerticalAlignment="Center">
                <Button Style="{DynamicResource FriendMenuNavigationButton}"  Content="Friends" Height="60" FontSize="25" Margin="0,20,0,10" VerticalAlignment="Center"/>
                <Button Style="{DynamicResource FriendMenuNavigationButton}" Content="Following"  Height="60" FontSize="25" Margin="0,10" VerticalAlignment="Center"/>
                <Button Style="{DynamicResource FriendMenuNavigationButton}" Content="Followers" Height="60" FontSize="25" Margin="0,10" VerticalAlignment="Center"/>
                <Button Style="{DynamicResource FriendMenuNavigationButton}" Content="Blocked" Height="60" FontSize="25" Margin="0,10" VerticalAlignment="Center"/>
                <Button Style="{DynamicResource FriendMenuNavigationButton}" Content="Requests"  Height="60" FontSize="25" Margin="0,60,0,10" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Main Content Area -->
            <Border Grid.Column="1" Background="White">
                <StackPanel>
                    <TextBlock FontWeight="Bold" Text="Content"/>
                    <!-- Display Friends, Following, Followers, Blocked, or Requests based on selection -->
                    <!-- You will populate this area dynamically based on user selection from the left side menu -->
                </StackPanel>
            </Border>

            <!-- Recent Friend Requests Section -->
            <StackPanel Grid.Column="2" MinWidth="200">
                <TextBlock FontWeight="Bold" Text="Recent Friend Requests"/>
                <!-- Display the 10 most recent friend requests -->
                <!-- You will populate this area dynamically -->
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
